import { SearchInput } from "./styled/SearchBox.styled";

export default function SearchBox(){
    return(
        <div>
            드롭다운 카테고리, 지역 해야해
            <SearchInput type="text" placeholder="🔎 검색아이콘 넣기"/>
        </div>
    );
}